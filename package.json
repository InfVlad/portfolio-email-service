{
  "name": "le-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --minify"
  },
  "dependencies": {
    "hono": "^4.5.11"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
